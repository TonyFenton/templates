{{ form_start(form) }}
    {{ form_widget(form) }}
<div>
    <button class="btn btn-success icon-floppy btn-form-1-of-2" type="submit" role="button">Save</button>
    <a class="btn btn-primary icon-reply btn-form-2-of-2" role="link" href="{{ path('homepage') }}">Back to the folders list</a>
</div>
{{ form_end(form) }}
