{% extends 'layout.html.twig' %}

{% block title %}Edit folder{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-sm-10">
            {% include "_breadcrumb.html.twig" with {items: [
                {href: path('homepage'), name: "Folders" },
                {href: path('homepage'), name: folder.name },
                {name: "Edit"}
            ]} %}
        </div>
        <div class="col-sm-2 text-center">
            <div class="breadcrumb-btn">
                {{ include('folder/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

    {{ include('folder/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
